{{- if .Values.image.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ template "openstad.image.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "openstad.labels" . | nindent 4 }}
    app.kubernetes.io/component: {{ template "openstad.image.fullname" . }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ template "openstad.image.fullname" . }}
  minReplicas: {{ .Values.image.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.image.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.image.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.image.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.image.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.image.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
